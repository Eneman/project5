<html>
    <body>
        <h1>{{ event.name }}</h1>
        {% for trame in event.trames %}
            <h3>{{ trame.name }}</h3>
            <p>Matos :
                {{ trame.matos }}</p>
            <hr>
        {% endfor %}
    </body>
</html>
